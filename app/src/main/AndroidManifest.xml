<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.agna.realmvp.realmvpsample">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:name=".app.App">
        <activity android:name=".MainActivity">

        </activity>
        <activity android:name=".ui.screen.book.BookActivity"/>
        <activity android:name=".ui.screen.catalog.CatalogActivityView">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.screen.test.TestActivity"/>
        <activity android:name=".ui.screen.splash.SplashActivityView"/>
        <activity android:name=".ui.screen.catalog.filter.FilterActivityView"/>
    </application>


</manifest>